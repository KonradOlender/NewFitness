@{
    ViewData["Title"] = "AddPlanowanie Page";
}
<div class="col-md-12">
    <div class="row">
        <div class="col-md-12">
            <h1>Lista posiłków</h1>
        </div>
        <div>
            @foreach (var item in ViewBag.posilki)
            {
                <h1>@item.nazwa</h1>
            }
        </div>
        
    </div>
</div>